package hello

import (
	"fmt"
)

func main() {
	fmt.Print("hello world")
	// let's create a modul to this project using (go mod init) it will become module of this project
	// go test command will also test dependency package / libs too
}
